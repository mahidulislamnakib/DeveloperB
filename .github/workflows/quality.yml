name: Repository Quality

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  markdown-quality:
    name: Markdown quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Check required root files
        run: |
          test -f README.md
          test -f ROADMAP.md
          test -f AGENTS.md
          test -f START-HERE.md

      - name: Check required directory indexes
        run: |
          test -f catalog/README.md
          test -f playbooks/README.md
          test -f prompts/README.md
          test -f architectures/README.md

      - name: Check Markdown files are not empty
        run: |
          empty_files=$(find . -name "*.md" -type f -size 0)
          if [ -n "$empty_files" ]; then
            echo "Empty Markdown files found:"
            echo "$empty_files"
            exit 1
          fi

      - name: Summary
        run: |
          echo "Repository quality checks passed."

  workspace-quality:
    name: Typecheck, format, lint, and test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          package-manager-cache: false

      - name: Install dependencies
        run: npm install

      - name: Run typecheck
        run: npm run typecheck

      - name: Run formatting and lint checks
        run: npx biome check .

      - name: Run Worker tests
        run: npm run test
